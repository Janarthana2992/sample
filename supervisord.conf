[supervisord]
nodaemon=true
logfile=/var/log/supervisor/supervisord.log
loglevel=info
pidfile=/var/run/supervisord.pid

[unix_http_server]
file=/var/run/supervisor.sock

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[supervisorctl]
serverurl=unix:///var/run/supervisor.sock

; ── Nginx ───────────────────────────────────────────────────────────────────
[program:nginx]
command=nginx -g "daemon off;"
autostart=true
autorestart=true
priority=10
stdout_logfile=/var/log/supervisor/nginx.stdout.log
stderr_logfile=/var/log/supervisor/nginx.stderr.log

; ── auth_service  (port 8001) ────────────────────────────────────────────────
[program:auth_service]
command=uvicorn app.main:app --host 0.0.0.0 --port 8001
directory=/services/auth_service
autostart=true
autorestart=true
priority=20
stdout_logfile=/var/log/supervisor/auth.stdout.log
stderr_logfile=/var/log/supervisor/auth.stderr.log

; ── product_service  (port 8002) ─────────────────────────────────────────────
[program:product_service]
command=uvicorn app.main:app --host 0.0.0.0 --port 8002
directory=/services/product_service
autostart=true
autorestart=true
priority=20
stdout_logfile=/var/log/supervisor/product.stdout.log
stderr_logfile=/var/log/supervisor/product.stderr.log

; ── cart_service  (port 8003) ────────────────────────────────────────────────
[program:cart_service]
command=uvicorn app.main:app --host 0.0.0.0 --port 8003
directory=/services/cart_service
autostart=true
autorestart=true
priority=20
stdout_logfile=/var/log/supervisor/cart.stdout.log
stderr_logfile=/var/log/supervisor/cart.stderr.log

; ── order_service  (port 8004) ───────────────────────────────────────────────
[program:order_service]
command=uvicorn app.main:app --host 0.0.0.0 --port 8004
directory=/services/order_service
autostart=true
autorestart=true
priority=20
stdout_logfile=/var/log/supervisor/order.stdout.log
stderr_logfile=/var/log/supervisor/order.stderr.log

; ── ai_service  (port 8005) ──────────────────────────────────────────────────
[program:ai_service]
command=uvicorn app.main:app --host 0.0.0.0 --port 8005
directory=/services/ai_service
autostart=true
autorestart=true
priority=20
stdout_logfile=/var/log/supervisor/ai.stdout.log
stderr_logfile=/var/log/supervisor/ai.stderr.log
